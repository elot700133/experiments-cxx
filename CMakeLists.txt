CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(example-c++)

#FIND_PACKAGE(LIBRARY)

INCLUDE_DIRECTORIES(
  ${LIBRARY_INCLUDE_DIR}
)

#ADD_SUBDIRECTORY(src)

SET(SOURCE
  foo.cpp
  bar.cpp
main.cpp
)
SET(CMAKE_CXX_FLAGS "-pthread -g -O3 -flto -std=c++0x -Winline")
ADD_EXECUTABLE(a.out ${SOURCE})

TARGET_LINK_LIBRARIES(a.out
  ${LIBRARY_LIBRARIES}
)
